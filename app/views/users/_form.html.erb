<%= form_with(model: user) do |form| %>
    <% if user.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being checked:</h2>

        <ul>
        <% user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
        </div>
    <% end %>

    <div class="fields">
        <%= form.text_field :first_name, id: :first_name, placeholder: "First name"  %>
        <%= form.text_field :last_name, id: :last_name, placeholder: "Last name"  %>
        <%= form.text_field :url, id: :url, placeholder: "Url" %>
        <%= form.submit "Search" %>
    </div>   
<% end %>
  